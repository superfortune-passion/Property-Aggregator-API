APP_ENV=dev
APP_SECRET=change_me_in_production

# Redis connection used by Symfony Cache component
REDIS_URL=redis://127.0.0.1:6379

# Aggregated properties cache TTL in seconds (default: 300)
PROPERTIES_CACHE_TTL=300
